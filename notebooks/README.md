# Notebooks

The platform is designed to run from the CLI first, but this folder includes a lightweight
walkthrough notebook for exploratory review.

Use notebooks for analysis and visualization, not for hidden pipeline logic. Any reusable
code should live under `src/quant_platform/` with tests.
